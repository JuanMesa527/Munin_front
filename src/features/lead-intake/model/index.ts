/** Barrel interno de `model/`. Consumido solo por `ui/` de esta misma feature. */
export {
  useIntakeConversation,
  type IntakePhase,
  type UseIntakeConversationResult,
} from './use-intake-conversation';
export { FIXTURE_TURNS } from './lead-intake.fixtures';
